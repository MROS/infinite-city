const ObjectId = require("mongoose").Schema.Types.ObjectId;
const db = require("../database.js");
let router = require("express").Router();

router.get("/rule/:article", async function(req, res) {
	
});

module.exports = router;